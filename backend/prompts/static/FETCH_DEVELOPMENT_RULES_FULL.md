# Fetch.ai Development Rules (full)

This file is intentionally **bounded at prompt-assembly time** via `PROMPT_FULL_RULES_MAX_CHARS` (default 12000 characters).

Paste or symlink the complete Innovation Labs-derived rules here for offline use, or rely on `INNOVATION_LABS_DOCS` + RAG for depth.

Quick links catalog: see `FETCH_OFFICIAL_LINKS.md` in this folder.

## Package pins (reference)

- `uagents==0.22.5`
- `langchain==0.3.23` / `langchain-openai==0.2.14`
- `langgraph==0.3.20`
- `uagents-adapter==0.4.0`

(Add the rest of your extended rules below.)
